# Suppress deprecation warnings from transitive dependencies
# (These are warnings only, not errors - build still succeeds)
fund=false
audit=false
loglevel=error
legacy-peer-deps=false
prefer-dedupe=true
